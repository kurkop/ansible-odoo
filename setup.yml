#- hosts: all
#  remote_user: vagrant
#  user: vagrant
- name: Install Common
  hosts: all
  sudo: yes
  remote_user: vagrant
  #remote_user: odoo
  #sudo-pass: jpa1123
  roles:
    - common
    - nginx
    - postgres
    - odoo
    - uwsgi
  #atasks:
    # This should work too / #2372
